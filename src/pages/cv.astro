---
import BaseLayout from "../layouts/BaseLayout.astro";
import SectionBackground from "../components/backgrounds/SectionBackground.astro";
import PageShell from "../components/layout/PageShell.astro";

const projectPlaceholders = [
  "Chr0nosV3rs",
  "VespΣr",
  "Lun△rMood",
  "Ph3yNyx.Studio",
];
const assetBase = import.meta.env.BASE_URL.endsWith("/")
  ? import.meta.env.BASE_URL
  : `${import.meta.env.BASE_URL}/`;
const internalBase = import.meta.env.DEV ? "/" : "/Ph3yNyx.Portfolio/";
const withBase = (path = "") => `${internalBase}${path}`;
const projectsHref = withBase("projets/");
const profilePhoto = `${assetBase}phey-rainville-profile.webp`;
const githubHref = "https://github.com/ph3ynyxstudio-source";
const linkedInHref =
  "https://www.linkedin.com/in/phey-rainville?utm_source=share_via&utm_content=profile&utm_medium=member_android";
const studioEmail = "ph3ynyx.studio@gmail.com";

const skillGroups = [
  "Frontend",
  "Applications locales",
  "Mobile",
  "Documentation",
  "Outils",
  "IA comme assistant",
];

const placeholderBullets = ["À compléter", "À compléter", "À compléter"];
---

<BaseLayout
  title="CV numérique | Ph3yNyx.Studio"
  description="Structure de base du CV numérique de Phey Rainville."
>
  <SectionBackground>
    <PageShell class="cv-page cv-hero-shell">
      <article class="glass-panel cv-hero-card">
        <div class="cv-hero-main">
          <div class="cv-avatar-wrap">
            <img
              class="cv-avatar"
              src={profilePhoto}
              alt="Photo de profil de Phey Rainville"
              loading="eager"
            />
          </div>

          <div class="cv-hero-copy">
            <p class="eyebrow">CV numérique</p>
            <h1 class="page-title cv-name">Phey Rainville</h1>
            <p class="cv-role">
              Créateur d’outils numériques · Développeur web junior
            </p>
            <div class="cv-meta-grid">
              <p><span>Localisation</span> Gatineau, Québec</p>
              <p>
                <span>Email</span>
                <a class="cv-inline-link" href={`mailto:${studioEmail}`}>{studioEmail}</a>
              </p>
              <p>
                <span>LinkedIn</span>
                <a class="cv-inline-link" href={linkedInHref} target="_blank" rel="noreferrer">
                  linkedin.com/in/phey-rainville
                </a>
              </p>
              <p>
                <span>GitHub</span>
                <a class="cv-inline-link" href={githubHref} target="_blank" rel="noreferrer">
                  github.com/ph3ynyxstudio-source
                </a>
              </p>
            </div>
          </div>
        </div>

        <div class="cv-hero-actions">
          <a class="button button-primary" href={projectsHref}>Voir mes projets</a>
        </div>
      </article>
    </PageShell>

    <PageShell class="cv-page">
      <div class="cv-section-stack">
        <section class="glass-panel cv-section-card">
          <p class="eyebrow">Objectif de carrière</p>
          <p class="cv-placeholder">
            Je cherche à intégrer une équipe où je pourrai faire mes preuves,
            apprendre dans un vrai contexte de travail et contribuer à des
            projets concrets.
          </p>
          <p class="cv-placeholder">
            Je vise un cadre stable et durable, avec un bon équilibre travail /
            vie personnelle, idéalement autour de 30 à 32 heures par semaine,
            avec une possibilité d’ajuster temporairement la charge lors de
            périodes importantes.
          </p>
          <p class="cv-placeholder">
            Mon objectif est de progresser graduellement, développer mes
            compétences et gagner en responsabilités avec le temps.
          </p>
        </section>

        <section class="glass-panel cv-section-card">
          <p class="eyebrow">Profil</p>
          <p class="cv-placeholder">
            Je crée des outils numériques pratiques, documentés et pensés pour
            aider à organiser, comprendre et reprendre des projets plus
            facilement.
          </p>
          <p class="cv-placeholder">
            Ma méthode repose sur une progression simple : clarifier le besoin,
            construire une première version utilisable, tester, documenter,
            puis améliorer par étapes.
          </p>
          <p class="cv-placeholder">
            J’utilise Chr0nosV3rs, mon propre outil local, pour documenter mes
            sessions de travail, garder une trace de mes blocages, de mes
            apprentissages et de ma progression.
          </p>
          <p class="cv-placeholder">
            J’utilise aussi l’IA comme assistant de réflexion pour structurer
            mes idées, comparer des pistes et accélérer certaines tâches, tout
            en gardant la décision humaine au centre.
          </p>
        </section>

        <section class="glass-panel cv-section-card">
          <div class="cv-section-head">
            <p class="eyebrow">Projets principaux</p>
            <a class="button button-secondary" href={projectsHref}>
              Voir plus sur mes projets
            </a>
          </div>

          <div class="cv-project-grid">
            {projectPlaceholders.map((project) => (
              <article class="cv-project-card">
                <h2>{project}</h2>
                <div class="cv-project-lines">
                  <p><span>Type</span> Type à compléter.</p>
                  <p><span>Résumé</span> Résumé à compléter.</p>
                  <p><span>Stack</span> Stack à compléter.</p>
                  <p>
                    <span>Compétences observables</span> Compétences à
                    compléter.
                  </p>
                </div>
              </article>
            ))}
          </div>
        </section>

        <section class="glass-panel cv-section-card">
          <p class="eyebrow">Compétences</p>
          <div class="cv-skill-grid">
            {skillGroups.map((group) => (
              <article class="cv-skill-card">
                <h2>{group}</h2>
                <p class="cv-placeholder">À compléter.</p>
              </article>
            ))}
          </div>
        </section>

        <section class="glass-panel cv-section-card">
          <p class="eyebrow">Atouts professionnels</p>
          <div class="cv-strength-grid">
            {placeholderBullets.map((item) => (
              <article class="cv-strength-card">
                <p>{item}</p>
              </article>
            ))}
          </div>
        </section>

        <section class="glass-panel cv-section-card">
          <p class="eyebrow">Expérience professionnelle</p>
          <div class="cv-timeline-card">
            <p><span>Rôle / domaine</span> À compléter.</p>
            <p><span>Entreprise ou contexte</span> À compléter.</p>
            <p><span>Période</span> À compléter.</p>
            <p><span>Points clés</span> À compléter.</p>
          </div>
        </section>

        <section class="glass-panel cv-section-card">
          <p class="eyebrow">Formation</p>
          <div class="cv-timeline-card">
            <p><span>Formation</span> À compléter.</p>
            <p><span>Établissement ou contexte</span> À compléter.</p>
            <p><span>Période</span> À compléter.</p>
            <p><span>Notes</span> À compléter.</p>
          </div>
        </section>

        <section class="glass-panel cv-section-card">
          <div class="cv-section-head">
            <p class="eyebrow">Liens / contact</p>
            <a class="button button-secondary" href={projectsHref}>
              Voir mes projets
            </a>
          </div>

          <div class="cv-contact-grid">
            <article class="cv-contact-card">
              <h2>Email</h2>
              <p class="cv-placeholder">
                <a class="cv-inline-link" href={`mailto:${studioEmail}`}>{studioEmail}</a>
              </p>
            </article>
            <article class="cv-contact-card">
              <h2>LinkedIn</h2>
              <p class="cv-placeholder">
                <a class="cv-inline-link" href={linkedInHref} target="_blank" rel="noreferrer">
                  linkedin.com/in/phey-rainville
                </a>
              </p>
            </article>
            <article class="cv-contact-card">
              <h2>GitHub</h2>
              <p class="cv-placeholder">
                <a class="cv-inline-link" href={githubHref} target="_blank" rel="noreferrer">
                  github.com/ph3ynyxstudio-source
                </a>
              </p>
            </article>
          </div>
        </section>
      </div>
    </PageShell>
  </SectionBackground>
</BaseLayout>

<style>
  .cv-page {
    padding-top: 2rem;
    padding-bottom: 0;
  }

  .cv-hero-shell {
    padding-top: 3rem;
  }

  .cv-section-stack {
    display: grid;
    gap: 1.25rem;
    padding-bottom: 5.5rem;
  }

  .cv-hero-card,
  .cv-section-card {
    padding: 1.4rem;
  }

  .cv-hero-card {
    display: grid;
    gap: 1.25rem;
  }

  .cv-hero-main {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    gap: 1.25rem;
    align-items: center;
  }

  .cv-avatar-wrap {
    width: 124px;
    height: 124px;
    border-radius: 999px;
    overflow: hidden;
    border: 1px solid rgba(126, 239, 255, 0.22);
    background: rgba(84, 214, 255, 0.06);
    flex-shrink: 0;
  }

  .cv-avatar {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .cv-hero-copy {
    display: grid;
    gap: 0.85rem;
    min-width: 0;
  }

  .cv-name {
    font-size: clamp(2.2rem, 5vw, 3.8rem);
  }

  .cv-role {
    color: var(--color-cyan-bright);
    font-weight: 600;
  }

  .cv-meta-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 0.7rem 1rem;
  }

  .cv-meta-grid p,
  .cv-project-lines p,
  .cv-timeline-card p,
  .cv-placeholder {
    color: var(--color-muted);
    white-space: normal;
    overflow-wrap: break-word;
    word-break: normal;
    line-height: 1.65;
  }

  .cv-meta-grid span,
  .cv-project-lines span,
  .cv-timeline-card span {
    display: block;
    margin-bottom: 0.2rem;
    color: var(--color-text);
    font-weight: 600;
  }

  .cv-inline-link {
    color: var(--color-cyan-bright);
    text-decoration: none;
    overflow-wrap: break-word;
    word-break: normal;
  }

  .cv-inline-link:hover,
  .cv-inline-link:focus-visible {
    text-decoration: underline;
  }

  .cv-hero-actions,
  .cv-section-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .cv-project-grid,
  .cv-skill-grid,
  .cv-strength-grid,
  .cv-contact-grid {
    display: grid;
    gap: 1rem;
  }

  .cv-project-grid {
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
  }

  .cv-skill-grid {
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
  }

  .cv-strength-grid,
  .cv-contact-grid {
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
  }

  .cv-project-card,
  .cv-skill-card,
  .cv-strength-card,
  .cv-contact-card,
  .cv-timeline-card {
    display: grid;
    gap: 0.75rem;
    padding: 1rem;
    border: 1px solid rgba(126, 239, 255, 0.14);
    border-radius: 14px;
    background: rgba(84, 214, 255, 0.04);
  }

  .cv-project-card h2,
  .cv-skill-card h2,
  .cv-contact-card h2 {
    margin: 0;
    font-size: 1.05rem;
    line-height: 1.2;
  }

  .cv-project-lines {
    display: grid;
    gap: 0.7rem;
  }

  .cv-strength-card p {
    color: var(--color-text);
    font-weight: 600;
  }

  @media (max-width: 768px) {
    .cv-page {
      padding-top: 1rem;
    }

    .cv-hero-shell {
      padding-top: 2rem;
    }

    .cv-hero-main {
      grid-template-columns: 1fr;
      justify-items: start;
    }

    .cv-meta-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 640px) {
    .cv-section-stack {
      padding-bottom: 4rem;
    }

    .cv-hero-card,
    .cv-section-card {
      padding: 1.15rem;
    }

    .cv-avatar-wrap {
      width: 108px;
      height: 108px;
    }
  }
</style>
