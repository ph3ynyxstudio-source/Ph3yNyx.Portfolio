---
import SquareCard from "./SquareCard.astro";

interface Props {
  title: string;
  description: string;
  icon: string;
}

const { title, description, icon } = Astro.props;
---

<SquareCard title={title} description={description} icon={icon} />
